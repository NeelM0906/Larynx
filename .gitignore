# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/

# uv
uv.lock.bak

# Env
.env
.env.local

# Data / outputs
/data/
/outputs/
/latent_cache/
/lora_weights/

# Read-only upstream reference checkouts — cloned for design reference
# only; not part of our build or git history. See docs/ORCHESTRATION-M7.md
# §0 for provenance.
/third_party/
*.wav
*.mp3
!packages/gateway/tests/fixtures/**/*.wav

# Local session state — Claude Code plugin cache + scratch clones.
# Never belongs in the repo; each developer's state is their own.
/.claude/
/my-clone/

# Soak / staging verification harness outputs — generated per run,
# not committed. The rendered reports (SOAK_REPORT.md / STAGING_VERIFICATION_REPORT.md)
# live at the repo root and are committed.
/soak-artifacts/
/staging-artifacts/

# Editor
.vscode/
.idea/
*.swp

# Docker
.docker/

# OS
.DS_Store
Thumbs.db
